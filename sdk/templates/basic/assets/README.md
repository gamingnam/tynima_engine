Models go here — `.gltf` or `.glb`. `tynima cook` turns them into the
`.tymodel` blobs the engine loads, in `cooked/`; `ty.model("assets/thing.glb")`
also cooks one on the way in while you are working.
